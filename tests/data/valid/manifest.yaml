output_dir: "datazen-out-test"

params:
  renders: "test"
  renders_str: "renders"
  compiles: "configs3"

includes:
- "includes/renders.yaml"

configs:
- "configs"
- "configs2"
variables:
- "variables"
- "variables2"

schemas:
- "custom_schemas"

default_target: "{{renders_str}}-b"

commands:
- name: "a"
  command: "ls"
  arguments:
  - "-lat"
- name: "false"
  command: "false"

groups:
- name: "test"
  dependencies:
  - "{{renders_str}}-a"
  - "{{renders_str}}-b"
  - "{{renders_str}}-test.md"
  - "{{renders_str}}-test.py"
