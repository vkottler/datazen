default_target: "groups-all"

params:
  project: "datazen"

includes:
- "config/includes/python.yaml"

configs:
- "local/configs"

templates:
- "local/templates"

variables:
- "local/variables"

compiles:
- name: "local"

renders:
- name: "README.md"
  output_dir: "."
  dependencies:
  - "compiles-local"

groups:
- name: "all"
  dependencies:
  - "renders-README.md"
  - "groups-python"
