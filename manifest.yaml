default_target: "groups-all"

params:
  project: "datazen"
  python_version: "3.8"
  entry: "dz"

includes:
- "config/includes/python.yaml"
- "config/includes/help.yaml"

configs:
- "local/configs"

templates:
- "local/templates"

variables:
- "local/variables"

compiles:
- name: "local"

renders:
- name: "README.md"
  output_dir: "."
  dependencies:
  - "compiles-local"
  - "commands-help"

groups:
- name: "all"
  dependencies:
  - "renders-README.md"
  - "groups-python"
