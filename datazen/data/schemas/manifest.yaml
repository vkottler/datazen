includes: paths

output_dir:
  type: string

cache_dir:
  type: string

configs:      paths
schemas:      paths
schema_types: paths
templates:    paths
variables:    paths

params:
  type: dict

default_target:
  type: string

compiles:
  type: list
  schema:
    type: dict
    schema:
      configs: paths
      schemas: paths
      variables: paths
      dependencies: deps
      name:
        type: string
      output_type:
        type: string
      output_dir:
        type: string
      index_path:
        type: string
      append:
        type: boolean

commands:
  type: list
  schema:
    type: dict
    schema:
      name:
        type: string
      daemon:
        type: boolean
      timeout:
        type: number
      dependencies: deps

renders:
  type: list
  schema:
    type: dict
    schema:
      templates: paths
      dependencies: deps
      name:
        type: string
      output_dir:
        type: string
      output_path:
        type: string
      no_dynamic_fingerprint:
        type: boolean

groups:
  type: list
  schema:
    type: dict
    schema:
      name:
        type: string
      dependencies: deps
