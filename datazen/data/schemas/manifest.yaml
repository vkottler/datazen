output_directory:
  type: "string"

configs:
  type: list
  schema:
    type: "string"

schemas:
  type: list
  schema:
    type: "string"

templates:
  type: list
  schema:
    type: "string"

variables:
  type: list
  schema:
    type: "string"

compiles:
  type: list
  schema:
    type: "dict"
    schema:
      configs:
        type: list
        schema:
          type: "string"
      schemas:
        type: list
        schema:
          type: "string"
      variables:
        type: list
        schema:
          type: "string"
      name:
        type: "string"
      output_type:
        type: "string"
      output_directory:
        type: "string"
      append:
        type: "boolean"

renders:
  type: list
  schema:
    type: "dict"
    schema:
      templates:
        type: list
        schema:
          type: "string"
      name:
        type: "string"
      output_directory:
        type: "string"
